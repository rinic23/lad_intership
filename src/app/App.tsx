import { TestComponent } from 'features';

import React from 'react';
import { LadIcon } from 'shared';

const App = () => (
  <div className="App">
    <TestComponent a={0} />
    <LadIcon />
  </div>
);

export default App;
